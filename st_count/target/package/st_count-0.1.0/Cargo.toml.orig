[package]
name = "st_count"
version = "0.1.0"
authors = ["osama qamar"]
edition = "2018"
discription = "library to count characters and special characters in a string like spaces, vovels, consonents e.t.c"
licence = "MIT"
repository = "https://github.com/Osama530/st_count.git" 

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
